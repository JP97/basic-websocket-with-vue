

const state = {
    connected: false,
    error: '',
    message: '',
};

const getters = {
    getConnectionState: (state) => state.connected
};

const actions = {
    // send({commit}, message){

    // }
};

const mutations = {

};


export default {
    state,
    getters,
    actions,
    mutations
}